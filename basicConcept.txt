📌 Coordinate APIs DONE
| Action            | Method | Endpoint                  | Description          |
| ----------------- | ------ | ------------------------- | -------------------- |
| Add coordinate    | POST   | `/api/coordinates`        | Add 1 point          |
| Delete coordinate | DELETE | `/api/coordinates/:id`    | Remove by lat/lon    |
| Clear all         | DELETE | `/api/coordinates`        | Remove all           |
| Upload CSV        | POST   | `/api/coordinates/upload` | Add many coordinates |


📌 Prediction APIs DONE
| Action            | Method | Endpoint                    | Description          |
| ----------------- | ------ | --------------------------- | -------------------- |
| Predict one       | POST   | `/api/predict/single`       | Predict 1 point      |
| Predict all       | POST   | `/api/predict/batch`        | Predict session list |
| View predictions  | GET    | `/api/predictions`          | List CSV predictions |
| Clear predictions | DELETE | `/api/predictions`          | Delete CSV           |
| Download CSV      | GET    | `/api/predictions/download` | File                 |

📌 Map APIs
| Method | Endpoint   | Description                 |
| ------ | ---------- | --------------------------- |
| GET    | `/api/map` | Return coordinates + center |


7. coordinate_routes.py
to be FIxed :
✔ FULLY reviewed
✔ Corrected version provided
⚠ FIXES applied:

wrong redirect

wrong access to result keys

consistent AJAX handling

proper _return_json usage
